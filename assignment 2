question Write a program that takes side of a square as input and calculates the area of this square by using area() function.
def area_square(n):      
    area = pow(n,2)    
    return area
    
num=float(input("Enter number") )
print("Sum of digits",area_square(num))
